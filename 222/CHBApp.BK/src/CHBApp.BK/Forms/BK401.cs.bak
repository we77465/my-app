using CHBApp.BK.Common;
using CHBApp.BK.Services;

namespace CHBApp.BK.Forms;

/// <summary>BK401 轉換磁片作業(舊規格--全體) - SCT 14 元件</summary>
public partial class BK401 : ExportFormBase
{
    public BK401()
    {
        InitializeComponent();
        btnOk.Click += (_, _) =>
            DoExport(BkacRepository.ValidForExport(), PccutExporter.Format.Old);
    }
}
